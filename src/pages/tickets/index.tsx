import Layout from "@/components/layout/Layout";
import TicketsSections from "@/components/tickets/TicketsSections";
import React from "react";

const TicketsPage = () => {
    return (
        <Layout>
            <TicketsSections />
        </Layout>
    );
};

export default TicketsPage;
